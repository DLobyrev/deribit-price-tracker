# Deribit Price Tracker

Сервис для периодического сбора index price BTC/USD и ETH/USD с биржи Deribit и предоставления данных через REST API.

## 📦 Требования

- Python 3.10+
- PostgreSQL
- Redis
- Docker (опционально, но рекомендуется)

## ⚙️ Установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/DLobyrev/deribit-price-tracker.git
   cd deribit-price-tracker
   
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt

3. Создайте базу данных в PostgreSQL:
   ```sql
   CREATE DATABASE deribit_db;
   
4. Настройте подключение в app/database.py (или через переменную окружения DATABASE_URL):
   ```python
   DATABASE_URL = "postgresql://postgres:ваш_пароль@localhost:5432/deribit_db"

## ▶️ Запуск

1. Запустите Redis (через Docker):
   ```bash
   docker run -d --name redis-local -p 6379:6379 redis:7-alpine

2. Запустите Celery Worker и Beat:
   ```bash
    # Терминал 1
    celery -A app.tasks:celery_app worker --loglevel=info --pool=solo
    
    # Терминал 2
    celery -A app.tasks:celery_app beat --loglevel=info
   
3. Запустите FastAPI-сервер:
   ```bash
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   
🌐 API Эндпоинты
Все эндпоинты требуют query-параметр ticker (btc_usd или eth_usd):

GET /prices/ — все сохранённые цены
GET /prices/latest/ — последняя цена
GET /prices/by-date/?date=YYYY-MM-DD — цены за указанную дату

Пример:
http://localhost:8000/prices/latest/?ticker=btc_usd

🧠 Design Decisions
Celery + Redis
Выбрано как простое и надёжное решение для периодических задач. Kafka не подходит, так как это stream-платформа, а не очередь задач. RabbitMQ возможен, но избыточен для низкой нагрузки.
Локальное время вместо timestamp от биржи
Deribit в эндпоинте get_index_price не всегда возвращает timestamp, поэтому фиксируем момент сохранения на стороне клиента.
SQLAlchemy ORM
Использован для читаемости и быстрой разработки. Для junior-проекта этого достаточно.
Без Pydantic-схем
Упрощено до dict для ускорения реализации, но в продакшене рекомендуется использовать модели.
--pool=solo для Celery на Windows
Решает проблемы с многопроцессностью в Windows-среде.